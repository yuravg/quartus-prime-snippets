# -*- mode: snippet -*-
# name: qpp-Always-Construct-(Sequential)
# key: qpp-Module-Items-Always-Construct-(Sequential)
# --
// Always Construct (Sequential)
// <edge_events> may contain any number of posedge or negedge events
// separated by "or", e.g. always@(posedge clk or negedge reset)
always@(<edge_events>)
begin
	// Statements
end
