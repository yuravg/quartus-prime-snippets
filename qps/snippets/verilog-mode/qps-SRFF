# -*- mode: snippet -*-
# name: qps-SRFF
# key: qps-Registers-and-Latches-SRFF
# --
// SRFF
	SRFF <instance_name> (
				.s(<synchronous_set>),
				.r(<synchronous_reset>),
				.clk(<clock_signal>),
				.clrn(<active_low_clear>),
				.prn(<active_low_preset>),
				.q(<data_out>)
				);
