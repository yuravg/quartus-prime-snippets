# -*- mode: snippet -*-
# name: qps-LATCH
# key: qps-Registers-and-Latches-LATCH
# --
// LATCH
-- Add the library and use clauses before the design unit declaration
library altera;
use altera.altera_primitives_components.all;
-- Instantiating LATCH
	<instance_name> : LATCH
	port map (
			d => <data_in>,
			ena => <clock_enable>,
			q => <data_out>
			);
